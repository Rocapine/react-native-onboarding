{"slug":"authoring-surfaces-drift-from-runtime","class":"docs-integrity","status":"open","firstSeen":"wf_48bf5783-6b3","runs":["wf_48bf5783-6b3"],"evidence":"4 findings in one run (r0-3, r2-3, r2-4, r2-5). `actions.pending.*` was not added to the uncaptured-variable exception lists in claude-plugin/skills/validate-step-json/SKILL.md:92 and step-json-reviewer.md:71, so the SDK's own validator flags the SDK's own new pattern as a bug — with the `products.*` carve-out two lines away as the precedent that was not followed. Two of the four authoring files CLAUDE.md:167 names still teach the shape this PR reclassifies as stranded. website/docs/customization/custom-actions.mdx:126 documents `retry: { maxAttempts }` with no idempotency warning, so its own network-gate example becomes three POSTs on a 500. The tier-3 rule at CLAUDE.md:167 documents its own decay in-line: 'The list of files in this rule was incomplete for months; prefer the check's output over this sentence, and treat website/ as the part still on trust.'","countermeasure":{"applied":null,"tier":2,"what":"CM1 — extend scripts/check-element-docs.mjs (already CI-gated at .github/workflows/build.yml:29) to assert every ButtonAction field and every runtime-projected variable namespace appears in the six authoring surfaces. Membership and naming only; prose stays hand-written, per the script's own header. Approved 2026-09-11, in flight.","where":"scripts/check-element-docs.mjs"}}
{"slug":"fix-round-introduces-unmeasured-regression","class":"verification-coverage","status":"open","firstSeen":"wf_48bf5783-6b3","runs":["wf_48bf5783-6b3"],"evidence":"r2-1 and r2-2, both introduced by the run's OWN fix loop and both still open at ship. f8e2cca ('read custom as AND for escapes') is stamped 09:17 UTC, inside fix round 1's window (08:57-09:23); round 2's commits f787f47 and 59c87b5 (10:09) refined the same barrier rather than catching it. r2-1: a `custom` in onGranted kills the plain \"continue\" in onDenied — measured two-worktree reproduction, base cb4a7d5 gives 1 onContinue call, head c613816 gives 0, user stranded with one console.error. It then survived verify round 2, BOTH review lenses in round 2 (8 findings, none about it), fix round 2, and verify round 3, and was caught only by review round 3 at 10:32 — over two hours and six reviewer-agents later. Nothing between Fix and Review compares behaviour to the merge base; verify is static + bundle only. NOTE: lowering fix.maxRounds would NOT have prevented this — round 1 introduced it. The indictment is detection, not round count.","countermeasure":{"applied":null,"tier":2,"what":"CM6 — require runtime evidence so a dead CTA is observable (a static suite structurally cannot see one). Longer-term the honest round-count lever is a stop condition, not a lower cap: escalate early when a round's findings concentrate in files the previous round's fix touched. Held until CM4 and CM6 land.","where":".claude/board.json verify.require (onboarding-studio), board-run.mjs verifyLoop"}}
{"slug":"logic-duplicated-across-packages","class":"conventions","status":"open","firstSeen":"wf_48bf5783-6b3","runs":["wf_48bf5783-6b3"],"evidence":"packages/onboarding/src/screens/completingActions.ts (280 lines) and packages/onboarding-ui/src/UI/Runtime/elements/completingActions.ts (125 lines) implement the same escape walk independently; neither imports the other, and TypeScript catches drift in neither direction because the UI package re-declares its own Zod schemas and prop types. RNO#191's headline semantics landed in only one of them (r1-5). Five findings trace to this 'wiring landed in one of N seams' class: r0-1, r0-7, r0-8, r1-5, fixer-1. The actionSchemaMirror guard over the duplication was itself found weak twice in the same run — r0-11 (compares names, not validators) and r1-8 (enumerates five cases of N). A guard over duplication is tier 2 dressed as tier 1; deleting the duplication is the tier 1. This is also the surface r2-1 and r2-2 sit on.","countermeasure":{"applied":null,"tier":1,"what":"CM4 — one source for the escape walk; the UI copy imports the headless one or both import a shared module, and actionSchemaMirror is then deleted rather than strengthened. Filed as #269, blocked by #191 (RNO!263 is actively editing both files).","where":"packages/onboarding/src/screens/completingActions.ts, packages/onboarding-ui/src/UI/Runtime/elements/completingActions.ts"}}
{"slug":"pr-context-refetched-per-agent","class":"round-trip-cost","status":"open","firstSeen":"wf_d65c5a9e-fe0","runs":["wf_48bf5783-6b3"],"evidence":"SLUG REUSED from publishing-platform's ledger, where its status is `applied` since 2026-09-10 — this is a proven tier-1 that was made opt-in per board (context.prContextCommand) and simply never ported to this repo, which has no scripts/pr-context.sh. 63 round trips spent re-fetching the same PR: gh pr view x19, git diff cb4a7d51... x18, git show pr263:<file> x15, gh pr diff x7, gh run list x4. Batch rate 6.5% at 1.06 calls/turn across 952 real turns, with the generic batching instruction already in force — so instructing harder was not the answer here either.","countermeasure":{"applied":null,"tier":1,"what":"CM2 — port publishing-platform's 96-line scripts/pr-context.sh and its CALL-COUNT test, and set context.prContextCommand in the board config. The spine already consumes the key at board-run.mjs:935; no spine edit needed. Approved 2026-09-11, in flight.","where":"scripts/pr-context.sh, .claude/board.json context.prContextCommand (onboarding-studio)"},"expected":"~63 of 1,013 tool calls, about 6%; ~$6 and ~4 agent-min per run of this shape."}
{"slug":"local-suite-has-no-aggregate-command","class":"round-trip-cost","status":"open","firstSeen":"wf_48bf5783-6b3","runs":["wf_48bf5783-6b3"],"evidence":"40 calls running the check suite one step at a time: npm run type:check x11, npm run build x10, npm test --workspace=packages/onboarding x8, --workspace=packages/onboarding-ui x7, npx vitest run x4. package.json has no root aggregate script, while .github/workflows/build.yml runs exactly seven steps. Several of the collapsed calls are themselves multi-minute, so this is the largest wall-clock win per unit of effort found in the run.","countermeasure":{"applied":null,"tier":1,"what":"CM3 — add a root `check` script whose steps are byte-identical to CI's seven, so 'green locally' and 'green in CI' become the same claim. Approved 2026-09-11, in flight.","where":"package.json"}}
{"slug":"followups-rederives-review-evidence","class":"round-trip-cost","status":"applied","firstSeen":"wf_48bf5783-6b3","runs":["wf_48bf5783-6b3"],"evidence":"The Follow-ups stage cost 39 min and $5.40 to file 6 issues — 70% of the build's wall clock — half of it in a killed first attempt that restarted from zero. The spine built its prompt from build.followUps + build.notVerified only, which are prose strings, while r.findings was already on `r` carrying summary, file, line, failureScenario, reasoning, disposition and filedAs. The stage re-derived every mechanism the review had already evidenced twice over, and the prompt's own 'verify your own claims' sentence is what instructed it to.","countermeasure":{"applied":"2026-09-11","tier":1,"what":"CM5 — template the filed and open findings into the Follow-ups prompt, and narrow 'verify your own claims' to claims the findings do not already evidence. Also widened the stage's early-return so findings alone are enough to run it; previously a run with findings but no surfaced prose skipped the stage entirely, which would have left the countermeasure inert. Lints clean.","where":"~/Developer/.claude/workflows/board-run.mjs followUpsStage"}}
{"slug":"no-runtime-evidence-for-ui-states","class":"verification-coverage","status":"open","firstSeen":"wf_48bf5783-6b3","runs":["wf_48bf5783-6b3"],"evidence":"4 of the 5 caveats on RNO!263. No screenshot of the pending, error or retry states exists on any surface; verification was static + bundle-level only (bundle/resolve/syntax/worklet-transform) and executes no JS, so nothing about whether the states render or transition was verified beyond what the vitest suites assert. The verifier's stated reason for skipping iOS was that a plain run:ios shows only the gate's idle state and the pending/error/retry states 'require interactive presses against a scheduled failure' — which is exactly what a Maestro flow does and exactly what a bare build cannot. The simulator-screenshoter agent (~/.claude/agents/simulator-screenshoter.md) now provides that, and the spine gate has existed unused at board-run.mjs:608-610 with noVisibleSurface:true as the honest escape hatch. NOTE: the @lottiefiles/dotlottie-react resolve failure (#230) breaks react-native-web ONLY; iOS is unaffected and that caveat is not a reason to decline the gate.","countermeasure":{"applied":null,"tier":2,"what":"CM6 — verify.require: [ci, screenshots] plus a verify.instructions clause naming simulator-screenshoter, and a rewrite of the now-false sentence claiming no simulator/Maestro infrastructure exists. Approved 2026-09-11, in flight. PRECONDITION: #270 (simulator contention) — every booted simulator was held by another session during this run, and until that is resolved the gate converts an honest caveat into a false blocker.","where":".claude/board.json verify.require (onboarding-studio)"}}
{"slug":"ticket-omits-error-contract","class":"ticket-quality","status":"open","firstSeen":"wf_48bf5783-6b3","runs":["wf_48bf5783-6b3"],"evidence":"All five of RNO!263's YOUR CALL items are semantics questions RNO#191's definition of done never answered, so no number of fix rounds could settle them — a triage escape, not a build escape. Same class produced the cross-repo findings r0-10, r1-6 and r1-7: an SDK atom shipped with no Studio authoring counterpart is a capability nobody can reach. CAVEAT ON THE COUNTERMEASURE, recorded honestly: CM7 cannot prevent r2-1 or r2-2, which are self-inflicted regressions rather than unanswered semantics, so it is weaker than its original billing of 'all 5 YOUR CALL blockers'.","countermeasure":{"applied":null,"tier":3,"what":"CM7 — triage instructions require a ticket introducing a new action field or runtime-projected namespace to state its error/abort contract and name the Studio authoring counterpart before build. Approved 2026-09-11, in flight. Tier 3 is the ceiling here and it WILL decay: CLAUDE.md:167 in this same repo is the evidence for how a remembered rule holds. Proposed because the alternative is nothing.","where":".claude/board.json triage.instructions (onboarding-studio)"}}
