{
  // Type-checks the test files, which `tsconfig.json` excludes so they never
  // reach `dist`. `npm run type:check:tests` — vitest itself does not type-check.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
